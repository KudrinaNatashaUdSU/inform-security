class VigenereCipher {
    constructor(text) {
        this.key = "";
        this.text = text;
    }

    encode() { // зашифровать
        return
    }

    decode() { // расшифровать
        return
    }
}